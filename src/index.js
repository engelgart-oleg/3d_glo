// Подключаем модули:
const timer = require('./modules/timer');
const menu = require('./modules/menu');
const modal = require('./modules/modal');

// Вызов модулей:
timer('10 March 2026');
menu();
modal();